venv/
__pycache__/
*.pyc
.env
.env.local
.DS_Store

# Credentials and secrets — never commit these
credentials.json
*.key
*.pem
*.p12
*.pfx

# Supabase local state
# Note: supabase.config.js is intentionally committed — it contains only the
# public anon key (sb_publishable_*) which is safe to expose in client-side code.
# RLS policies enforce data security server-side.
supabase/.env
supabase/.env.local
supabase/.temp/

# Node
node_modules/
npm-debug.log*
